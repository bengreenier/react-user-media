# React + TypeScript + Vite

Example app for `@bengreenier/react-user-media`.

```bash
pnpm --filter examples dev
```

Linting and formatting are handled by Biome at the monorepo root (`pnpm lint` / `pnpm format`).
